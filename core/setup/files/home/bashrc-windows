if [[ -n $PS1 ]]; then
	_gitdir=${HOME}/work/sync/git
	[[ -z $MSYSTEM ]] && _gitdir=/cygdrive/${_gitdir}
	source ~/.metasystem-config
	. ${_gitdir}/github/metasystem/home/shrc
	metasystem_cd ~
	unset _gitdir
fi

